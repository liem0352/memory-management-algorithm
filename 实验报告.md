#操作系统实训4：可变式分区存储管理

##1. 使用的算法

本实验实现了三种可变式分区分配算法：

###1.1 首次适应算法（First Fit）
-**原理**：从空闲区表的第一个空闲区开始查找，找到第一个能满足大小要求的空闲区即分配
-**优点**：分配速度快，查找简单
-**缺点**：容易在内存低端产生大量小的碎片

###1.2 最佳适应算法（Best Fit）
-**原理**：从所有空闲区中找出能满足要求的最小空闲区进行分配
-**优点**：尽量保留大的空闲区，减少大作业无法分配的情况
-**缺点**：容易产生大量小的碎片，查找时间较长

###1.3 最坏适应算法（Worst Fit）
-**原理**：从所有空闲区中找出最大的空闲区进行分配
-**优点**：分配后剩余的空闲区较大，可以满足后续较大作业的需求
-**缺点**：破坏大的空闲区，可能导致大作业无法分配

##2. 算法流程图

###2.1 内存分配流程图
```
开始
  ↓
选择分配算法
  ↓
输入作业ID和内存大小
  ↓
根据算法查找合适空闲区
  ↓
找到空闲区？
  ├─否→输出分配失败，结束
  └─是↓
检查空闲区大小
  ├─等于→直接分配
  └─大于↓
分割空闲区
  ↓
更新空闲区表和已分配表
  ↓
输出分配成功信息
  ↓
结束
```

###2.2 内存回收流程图
```
开始
  ↓
输入要回收的作业ID
  ↓
在已分配表中查找作业
  ├─未找到→输出回收失败，结束
  └─找到↓
将对应分区状态改为空闲
  ↓
从已分配表中删除该作业
  ↓
合并相邻空闲分区
  ↓
输出回收成功信息
  ↓
结束
```

###2.3 空闲分区合并流程图
```
开始
  ↓
遍历空闲区表
  ↓
检查当前分区与下一分区是否相邻且都空闲
  ├─否→继续检查下一分区
  └─是↓
合并两个分区
  ↓
删除下一分区
  ↓
空闲区数量减1
  ↓
重新检查当前位置
  ↓
继续遍历直到结束
  ↓
结束
```

##3. 代码片段

###3.1 数据结构定义
```c
// 分区结构体
typedef struct {
  int start_addr;  // 起始地址
  int size;  // 分区大小
  int status;  // 状态: 0-空闲, 1-已分配
  int job_id;  // 作业ID，空闲时为-1
} MemoryBlock;

// 全局变量
MemoryBlock free_blocks[MAX_BLOCKS];  // 空闲区表
MemoryBlock allocated_blocks[MAX_BLOCKS]; // 已分配表
int free_count = 0;  // 空闲区数量
int allocated_count = 0; // 已分配区数量
```

###3.2 首次适应算法实现
```c
int first_fit(int size) {
  for (int i = 0; i < free_count; i++) {
  if (free_blocks[i].status == 0 && free_blocks[i].size >= size) {
  return i;  // 返回第一个满足条件的空闲区索引
  }
  }
  return -1;  // 未找到合适的空闲区
}
```

###3.3 最佳适应算法实现
```c
int best_fit(int size) {
  int best_index = -1;
  int min_size = MAX_MEMORY + 1;
  
  for (int i = 0; i < free_count; i++) {
  if (free_blocks[i].status == 0 && free_blocks[i].size >= size) {
  if (free_blocks[i].size < min_size) {
  min_size = free_blocks[i].size;
  best_index = i;
  }
  }
  }
  return best_index;
}
```

###3.4 内存分配核心代码
```c
int allocate_memory(int job_id, int size) {
  int block_index = -1;
  
  // 根据选择的算法查找合适的分区
  switch (current_algorithm) {
  case 1: block_index = first_fit(size); break;
  case 2: block_index = best_fit(size); break;
  case 3: block_index = worst_fit(size); break;
  }
  
  if (block_index == -1) {
  printf("分配失败：没有足够的连续内存空间！\n");
  return 0;
  }
  
  // 检查是否需要分割分区
  if (free_blocks[block_index].size > size) {
  // 创建新的空闲分区并插入
  MemoryBlock new_block;
  new_block.start_addr = free_blocks[block_index].start_addr + size;
  new_block.size = free_blocks[block_index].size - size;
  new_block.status = 0;
  new_block.job_id = -1;
  
  // 更新原分区大小
  free_blocks[block_index].size = size;
  
  // 插入新分区到空闲区表
  for (int i = free_count; i > block_index + 1; i--) {
  free_blocks[i] = free_blocks[i-1];
  }
  free_blocks[block_index + 1] = new_block;
  free_count++;
  }
  
  // 更新分区状态并添加到已分配表
  free_blocks[block_index].status = 1;
  free_blocks[block_index].job_id = job_id;
  allocated_blocks[allocated_count] = free_blocks[block_index];
  allocated_count++;
  
  return 1;
}
```

###3.5 空闲分区合并代码
```c
void merge_free_blocks() {
  for (int i = 0; i < free_count - 1; i++) {
  if (free_blocks[i].status == 0 && free_blocks[i + 1].status == 0 &&
  free_blocks[i].start_addr + free_blocks[i].size == free_blocks[i + 1].start_addr) {
  
  // 合并分区
  free_blocks[i].size += free_blocks[i + 1].size;
  
  // 删除后面的分区
  for (int j = i + 1; j < free_count - 1; j++) {
  free_blocks[j] = free_blocks[j + 1];
  }
  free_count--;
  i--; // 重新检查当前位置
  }
  }
}
```

##4. 实验结果分析及体会

###4.1 实验结果

通过运行程序，我们成功实现了可变式分区存储管理的三种算法：

1. **首次适应算法**：分配速度快，但容易在内存低端产生碎片
2. **最佳适应算法**：能够找到最合适的分区，但会产生大量小碎片
3. **最坏适应算法**：保留较大的剩余空间，但可能破坏大空闲区

程序能够正确处理内存分配、回收和分区合并，实现了完整的存储管理功能。

###4.2 算法比较

| 算法 | 分配速度 | 碎片处理 | 大作业适应性 | 综合评价 |
|------|----------|----------|--------------|----------|
| 首次适应 | 快 | 一般 | 一般 | 适合一般场景 |
| 最佳适应 | 慢 | 差 | 好 | 适合小作业多的情况 |
| 最坏适应 | 中等 | 好 | 差 | 适合大作业多的情况 |

###4.3 实验体会

1. **算法选择的重要性**：不同的分配算法适用于不同的应用场景。在实际系统中，需要根据作业的特点选择合适的算法。

2. **内存碎片问题**：可变式分区管理虽然比固定分区更灵活，但仍然存在碎片问题。通过分区合并可以部分缓解，但不能完全消除。

3. **数据结构设计**：合理的数据结构设计是算法实现的基础。本实验中使用的空闲区表和已分配表能够有效管理内存状态。

4. **边界条件处理**：在实现过程中需要特别注意各种边界条件，如分区合并、分割时的地址计算等。

5. **实际应用考虑**：实际的操作系统内存管理比这个实验更复杂，还需要考虑虚拟内存、页面置换等机制。

###4.4 改进方向

1. 可以添加内存紧凑功能，将所有空闲区合并为一个大的空闲区
2. 可以增加更多的统计信息，如内存利用率、碎片率等
3. 可以实现更复杂的分配策略，如循环首次适应算法
4. 可以添加图形界面，更直观地显示内存分配情况

通过本次实验，我深入理解了可变式分区存储管理的原理和实现方法，对操作系统内存管理有了更深入的认识。